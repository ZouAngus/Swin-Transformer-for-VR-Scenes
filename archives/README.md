# attic/

Superseded or historical files kept for reference only — nothing here is part
of the current pipeline.

- `long_video_demo_4cls.py`, `long_video_4cls.py`, `long_video_demo_4cls_v2_pre_burst.py`
  — earlier iterations of the 4-class long-video demo. Current version:
  `demo/long_video_demo_4cls_v2.py`.
- `*.backup_*` — automatic backups taken before edits.
- `readme_server_legacy.md` — the original server-era readme (superseded by the
  top-level README.md).
- `action.json`, `04-11_action.json` — example inference outputs from early runs.

- `v3_known_good/` — frozen config + split of the v3 4-class fine-tune.
- `action.json`, `04-11_action.json` — legacy example outputs (moved from repo root).
